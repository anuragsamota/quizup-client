# Development environment variables for Vite client
# Set these to match your running backend services (ports used by the services)
VITE_QUIZ_MANAGE_API_BASE=http://localhost:8080
VITE_QUIZ_RUNTIME_API_BASE=http://localhost:5642

# Vite dev server port (optional)
VITE_PORT=5173
